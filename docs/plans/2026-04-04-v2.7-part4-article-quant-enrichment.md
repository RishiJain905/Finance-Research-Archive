# V2.7 Part 4 Article-Quant Enrichment Implementation Plan

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** Fix schema-field mismatch and verify complete V2.7 Part 4 implementation with all 81+ tests passing.

**Architecture:** The implementation is already 95% complete. This plan fixes the schema/script field name mismatch, verifies all spec requirements are met, and ensures full test coverage. The system uses config-driven deterministic linking with standalone link files and record enrichment.

**Tech Stack:** Python 3.11+, unittest, jsonschema, pathlib, hashlib

---

## Context for Implementer

The V2.7 Part 4 system is already built and functional with 81 passing tests. The codebase includes:

- **V2 Script:** `scripts/link_article_quant.py` (1351 lines) - Creates standalone link files, supports `--dry-run` and `--no-enrich` CLI flags
- **V1 Script:** `scripts/link_article_and_quant_records.py` (659 lines) - Modifies records in-place (coexists, not modified)
- **Test Suite:** 4 files (phase1: schema/config, phase2: core linking, phase3: enrichment, integration: end-to-end)
- **Config:** `config/quant_linking_rules.json` with topic_to_series, keyword_to_series, scoring_bands, dimension_weights

**Critical Pattern to Follow:**
```python
BASE_DIR = Path(__file__).resolve().parent.parent
CONFIG_PATH = BASE_DIR / "config" / "quant_linking_rules.json"
ACCEPTED_DIR = BASE_DIR / "data" / "accepted"
OUTPUT_DIR = BASE_DIR / "data" / "article_quant_links"
```

**Quant Identification:**
```python
QUANT_SERIES_IDS = frozenset({"sofr", "fed_funds", "iorb", "2y_yield", "10y_yield", "treasury_auctions", "upcoming_treasury_auctions", "repo_operations"})
QUANT_SOURCE_TYPES = frozenset({"quant_snapshot", "dataset_snapshot"})
```

**Link Structure (what script actually creates):**
```json
{
  "link_id": "link_abc123...",
  "article_id": "article_record_id",
  "quant_id": "quant_record_id",
  "event_id": "event_id_or_null",
  "relationship": "supports|context|weak_context",
  "score": 85,
  "matched_dimensions": ["topic", "time_window", "keyword_overlap", "event_alignment"],
  "created_at": "2026-04-03T00:00:00Z"
}
```

**Test Infrastructure:**
- Framework: `unittest` (NOT pytest, except phase1 which uses pytest for jsonschema)
- Run: `python -m unittest discover tests -p "test_link_article_quant_v2*.py" -v`
- Tests use `tempfile.TemporaryDirectory()` for isolation
- Tests copy real config from actual config path

---

## Task 1: Fix Schema Field Name Mismatch

**Files:**
- Modify: `schemas/article_quant_link.json`
- Test: `tests/test_link_article_quant_v2_phase1.py` (verify schema tests still pass)

**Step 1: Read current schema**

Read `schemas/article_quant_link.json` to see current field names.

**Step 2: Update schema field names**

Change `article_record_id` to `article_id` and `quant_record_id` to `quant_id` to match the script and all existing tests:

```json
{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Article Quant Link",
  "description": "Standalone link file schema for article-quant relationships",
  "type": "object",
  "required": ["link_id", "article_id", "quant_id", "relationship", "score", "matched_dimensions", "created_at"],
  "properties": {
    "link_id": {"type": "string"},
    "article_id": {"type": "string"},
    "quant_id": {"type": "string"},
    "event_id": {"type": "string"},
    "relationship": {"type": "string", "enum": ["supports", "context", "confirms", "weak_context"]},
    "score": {"type": "number", "minimum": 0, "maximum": 100},
    "matched_dimensions": {"type": "array", "items": {"type": "string", "enum": ["topic", "time_window", "keyword_overlap", "event_alignment"]}},
    "created_at": {"type": "string", "format": "date-time"}
  }
}
```

**Step 3: Run tests to verify schema change doesn't break anything**

Run: `python -m unittest discover tests -p "test_link_article_quant_v2*.py" -v`
Expected: All 81 tests still pass (script already uses correct field names)

**Step 4: Commit**

```bash
git add schemas/article_quant_link.json
git commit -m "fix: align schema field names with script implementation (article_id/quant_id)"
```

---

## Task 2: Verify Spec Completeness

**Files:**
- Read: `docs/V-2.7/V2_7_Part_4_Implementation_Plan.md`
- Read: All deliverables listed in spec

**Step 1: Check all spec deliverables exist**

Verify each deliverable from spec Section 8:
- [x] `schemas/article_quant_link.json` — EXISTS (fixed in Task 1)
- [x] `config/quant_linking_rules.json` — EXISTS
- [x] `scripts/link_article_quant.py` — EXISTS (1351 lines)
- [x] `data/article_quant_links/` — EXISTS (with .gitkeep)
- [x] Accepted record enrichment logic — EXISTS in script
- [x] Event cluster enrichment (article_links field) — EXISTS in schema
- [x] Test suite — EXISTS (4 files, 81 tests)

**Step 2: Verify Definition of Done criteria**

Check each criterion from spec:
1. ✅ Accepted article and quant records can be linked — `link_all_records()` function exists
2. ✅ Relationship files persisted in `data/article_quant_links/` — `save_link()` writes standalone files
3. ✅ Accepted records include cross-lane context — `enrich_accepted_record()` adds quant_context/article_context
4. ✅ Event clusters carry both narrative and numeric evidence — `enrich_event_cluster()` adds article_links + quant_links
5. ✅ Archive more useful for explanatory finance retrieval — Links enable narrative+quant queries

**Step 3: Verify script has all required functions from spec**

Check `scripts/link_article_quant.py` contains:
- [x] `load_accepted_records(base_dir)` — Task B1
- [x] `is_quant_record(record)` — Task B1
- [x] `is_article_record(record)` — Task B1
- [x] `get_topic_series(topic, config)` — Task B2
- [x] `compute_topic_score(article, quant, config)` — Task B2
- [x] `compute_time_window_score(article_date, quant_date, config)` — Task B3
- [x] `get_keyword_series(keywords, config)` — Task B4
- [x] `compute_keyword_overlap_score(article, quant, config)` — Task B4
- [x] `compute_event_alignment_score(article, quant, events)` — Task B5
- [x] `load_events(event_dir)` — Task B5
- [x] `compute_link_score(article, quant, events, config)` — Task B6
- [x] `classify_relationship(score, config)` — Task B6
- [x] `create_link(article_id, quant_id, event_id, relationship, score, dimensions)` — Task B6
- [x] `save_link(link, output_dir)` — Task B7
- [x] `load_links(output_dir)` — Task B7
- [x] `generate_link_id(article_id, quant_id)` — Task B7
- [x] `link_all_records(articles, quants, events, config, output_dir)` — Task B8
- [x] `main()` — Task B8
- [x] `enrich_accepted_record(record, links, link_type)` — Task C1
- [x] `write_enriched_record(record, base_dir)` — Task C1
- [x] `enrich_event_cluster(event, article_links, quant_links)` — Task C2

**Step 4: Verify test coverage matches spec**

Check `tests/` contains tests for all spec requirements:
- [x] TestLinkSchema — phase1
- [x] TestQuantLinkingRulesConfig — phase1
- [x] TestTopicSeriesMapping — phase2
- [x] TestKeywordSeriesMapping — phase2
- [x] TestTimeWindowScoring — phase2
- [x] TestKeywordOverlapScoring — phase2
- [x] TestEventAlignmentScoring — phase2
- [x] TestCombinedScoring — phase2
- [x] TestRelationshipClassification — phase2
- [x] TestLinkCreation — phase2
- [x] TestLinkPersistence — phase2
- [x] TestLinkDeduplication — integration
- [x] TestArticleToQuantLinking — integration
- [x] TestQuantToArticleLinking — integration
- [x] TestEventLinkedRecords — integration
- [x] TestRecordEnrichment — phase3 + integration
- [x] TestEventClusterEnrichment — phase3 + integration
- [x] TestEndToEnd — integration

**Step 5: No code changes needed if all checks pass**

This task is verification only. If everything checks out, proceed to Task 3.

---

## Task 3: Run Full Verification Suite

**Files:**
- All test files
- Script execution

**Step 1: Run all V2.7 tests**

Run: `python -m unittest discover tests -p "test_link_article_quant_v2*.py" -v`
Expected: All 81+ tests pass

**Step 2: Run full test suite to confirm no regressions**

Run: `python -m unittest discover tests -v`
Expected: All tests pass (no regressions in existing functionality)

**Step 3: Run linking script in dry-run mode**

Run: `python scripts/link_article_quant.py --dry-run`
Expected: Shows link preview without writing files

**Step 4: Run linking script for real**

Run: `python scripts/link_article_quant.py`
Expected: Creates link files in `data/article_quant_links/`, enriches accepted records

**Step 5: Verify output**

Check:
- `data/article_quant_links/` contains `.json` link files
- Enriched accepted records have `quant_context` or `article_context` blocks
- Link files conform to schema (validate against `schemas/article_quant_link.json`)

**Step 6: Commit if script run produces expected output**

```bash
git add data/article_quant_links/*.json
git commit -m "feat: generate initial article-quant links with V2.7 linking script"
```

---

## Task 4: Final Integration and Cleanup

**Files:**
- All modified files
- Git worktrees/branches

**Step 1: Verify no conflicts with V1 script**

Confirm `scripts/link_article_and_quant_records.py` is unchanged and still works.

**Step 2: Quality review checklist**

Per spec Section 7:
- [x] No duplicated logic between new and existing linking scripts
- [x] Consistent naming conventions (snake_case)
- [x] All new files follow existing patterns (BASE_DIR, load_json, save_json)
- [x] No leftover debug code or temporary scaffolding
- [x] Schema files are valid JSON Schema draft-07
- [x] Config files are valid JSON

**Step 3: Final test run**

Run: `python -m unittest discover tests -p "test_link_article_quant_v2*.py" -v`
Expected: All tests pass

**Step 4: Document completion**

Update any tracking docs if they exist, confirm all Definition of Done criteria met.

---

## Execution Commands Reference

```bash
# Create branch (if not already on one)
git checkout -b v2.7-part4-article-quant-enrichment

# Run V2.7 tests
python -m unittest discover tests -p "test_link_article_quant_v2*.py" -v

# Run full suite
python -m unittest discover tests -v

# Run linking script
python scripts/link_article_quant.py --dry-run
python scripts/link_article_quant.py

# Validate schema (if jsonschema installed)
python -c "import jsonschema, json; jsonschema.validate(json.load(open('data/article_quant_links/first_link.json')), json.load(open('schemas/article_quant_link.json')))"
```

---

## Risk Mitigation

| Risk | Mitigation |
|------|------------|
| Schema change breaks existing tests | Tests already use script's field names, schema update aligns with reality |
| V1 script conflicts | V1 script is untouched, uses different output mechanism |
| Event cluster schema change | Additive only (`article_links` field), `additionalProperties: true` |
| Link deduplication on re-run | Deterministic link_id, existing link check before creation |

---

## Definition of Done

1. ✅ Schema field names match script implementation
2. ✅ All 81+ tests passing
3. ✅ No regressions in existing tests
4. ✅ Link files created in `data/article_quant_links/`
5. ✅ Enriched accepted records have context blocks
6. ✅ Event clusters have both article_links and quant_links
7. ✅ Full test suite passes
8. ✅ Code committed cleanly
